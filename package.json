{
  "name": "fitflow",
  "private": true,
  "description": "FitFlow — npm workspaces: frontend app + scripts",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w fitflow-frontend",
    "build": "npm run build -w fitflow-frontend",
    "preview": "npm run preview -w fitflow-frontend",
    "lint": "npm run lint -w fitflow-frontend"
  }
}
